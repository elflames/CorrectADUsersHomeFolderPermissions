CorrectADUsersHomeFolderPermissions
===================================

This script will loop through /Users/ &amp; attempt to get the UniqueID for each user account it finds, if a User is found then the home folders permissions are propagated correctly.. else the permissions are set to read write for all